knowledge_impact: no
summary: Replace the timetable history dropdown with explicit version cards and exact-version View/Delete actions.
affected_areas:
  - timetable
  - tests
kms_files_updated:
no_impact_reason: This is a focused presentation correction implementing the already-documented rule that technical version selection and management belong only in Timetable History; permissions, lifecycle, deletion eligibility, and active-pointer behavior are unchanged.
major_change_override: no
