knowledge_impact: no
summary: "PostgreSQL compatibility hotfix for the already-governed Talent reassessment migration. The migration now emits a PostgreSQL-native TRUE boolean default instead of integer 1; no product behavior, architecture, schema intent, permissions, analytics semantics, or approved KMS decisions change."
affected_areas:
  - database-migrations
  - talent-student-assessment
kms_files_updated:
no_impact_reason: "Implementation compatibility correction only: BOOLEAN DEFAULT TRUE replaces BOOLEAN DEFAULT 1 on PostgreSQL while SQLite retains DEFAULT 1. The governed migration intent and resulting column semantics are unchanged."
major_change_override: no
