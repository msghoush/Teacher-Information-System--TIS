knowledge_impact: no
summary: Correct the Stage 3.5 timetable settings Jinja dictionary-key lookup and add route-render regression coverage.
affected_areas:
  - timetable
  - configuration-ui
  - tests
kms_files_updated:
no_impact_reason: This fixes only a Jinja dictionary-key collision and adds rendering coverage; timeline behavior and documented architecture are unchanged.
major_change_override: yes
