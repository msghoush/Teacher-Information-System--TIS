knowledge_impact: no
summary: Fix Planning section reopen behavior after Remove demand by preserving the targeted expanded section across pageshow and correcting the Planning return_to path. No new business rule, schema, architecture, permission, or lifecycle behavior is introduced.
affected_areas:
  - planning
  - ui-ux
  - tests
kms_files_updated:
no_impact_reason: Isolated frontend/navigation bug fix for already-documented behavior. The existing shared return_to/fragment convention and Remove-demand workflow are unchanged; this correction only prevents a later Planning pageshow handler from collapsing the section again.
major_change_override: yes
