knowledge_impact: no
summary: Fix existing-workspace plan selection so an explicit billing interval controls displayed options while immutable payment review remains tied to its stored quote.
affected_areas:
  - existing-workspace-paid-activation
  - organization-account
  - activation-plan-selection
kms_files_updated:
no_impact_reason: This is a narrow defect correction to the already documented Choose-a-Plan lifecycle. It distinguishes an explicit interval query from the saved draft default and keeps explicit activation review bound to its stored quote. It changes no lifecycle rule, payment authority, Paddle behavior, pricing, schema, migration, promo flow, branch data, or customer workflow.
major_change_override: yes
