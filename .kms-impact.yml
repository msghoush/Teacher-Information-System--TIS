knowledge_impact: no
summary: Correct the temporary read-only Al-Andalus production diagnostic to recognize the exact deployed SchoolGroup name.
affected_areas:
  - production-diagnostics
  - legacy-tenant-audit
kms_files_updated:
no_impact_reason: This correction changes only the standalone diagnostic's exact target name and normalized candidate key plus its static assertion. The script remains manually invoked, read-only, isolated from runtime application code, and changes no schema, data, Paddle behavior, permissions, or customer workflow.
major_change_override: yes
