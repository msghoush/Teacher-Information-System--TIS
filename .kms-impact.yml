knowledge_impact: no
summary: "Talent Rubric compatibility/UI defect fixes. Rubric mode selects the newest Draft after Start Editing, and legacy all-Grade competencies plus legacy shared rubric levels are projected into the new Grade -> Competency -> Rubric -> Levels tree so existing Mental Math data remains visible and editable. No product requirements, schema intent, API contract, permissions, analytics semantics, or governed architecture change."
affected_areas:
  - talent-rubric-ui
  - talent-program-setup
  - talent-rubric-legacy-compatibility
kms_files_updated:
no_impact_reason: "Implementation compatibility correction only. The approved Grade -> Competency -> competency-owned Rubric -> Levels workflow is unchanged. This patch fixes Draft selection and renders pre-existing all-Grade/shared-rubric data inside that already-approved hierarchy; editing the legacy rubric continues to use the existing backend compatibility bridge that copies it into a competency-owned rubric."
major_change_override: no
