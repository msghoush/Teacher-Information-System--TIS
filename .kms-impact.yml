knowledge_impact: no
summary: Fix unhandled IntegrityError on Planning section delete with a safe read-only pre-check; correct its message and the Subjects delete message so neither falsely promises that retiring Planning demand makes the record deletable, since retirement never deletes the row and the FK still blocks either way.
affected_areas:
  - planning
  - subjects
  - error-handling
  - tests
kms_files_updated:
no_impact_reason: Isolated bug fix plus a messaging-precision improvement inside routers/planning.py and routers/subjects.py. No architecture, schema, migration, permission, tenant/branch/year-scoping, or business-rule change; every existing FK protection, deletion policy, and the authoritative Curriculum Adjustment retirement workflow are unchanged, only surfaced safely instead of raising an unhandled 500. routers/ is conservatively auto-classified as major, hence this override.
major_change_override: yes
