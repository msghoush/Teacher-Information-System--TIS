knowledge_impact: no
summary: Remove the repeated TIS platform logo from the shared application header and let the existing page title occupy the left header identity position.
affected_areas:
  - shared-application-header-branding
kms_files_updated:
no_impact_reason: This is a presentation-only shell adjustment. It does not change product behavior, architecture, permissions, tenant isolation, data contracts, lifecycle rules, or approved KMS policy.
major_change_override: no
