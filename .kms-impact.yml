knowledge_impact: no
summary: "PostgreSQL compatibility hotfix for the already-governed Talent reassessment migration. The migration now emits a PostgreSQL-native TRUE boolean default instead of integer 1; no product behavior, architecture, schema intent, permissions, analytics semantics, or approved KMS decisions change."
affected_areas:
  - database-migrations
  - talent-student-assessment
kms_files_updated:
no_impact_reason: "db_migrations.py is a major-change path, but this patch does not alter migration intent or schema semantics; it only expresses the already-approved is_current BOOLEAN NOT NULL default using PostgreSQL-valid TRUE instead of integer 1, while SQLite retains DEFAULT 1. Product behavior and the resulting schema meaning are unchanged."
major_change_override: yes
