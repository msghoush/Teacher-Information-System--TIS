knowledge_impact: no
summary: "Owner-reproduced Student Assessments P0 follow-up: clicking Open Evaluation could fail server-side while the only exact backend error was written to the bottom operation message / screen-reader status, making the action appear to do nothing in the visible viewport. The Student Assessments operational feedback region is now rendered at the top of the workspace and the Open Evaluation action immediately shows 'Opening evaluation...' before the existing governed POST /api/talent/assessment-cycles/{id}/open call. Any backend rejection continues to surface its exact authoritative detail message through the existing operation error path; no authorization, lifecycle, schema, migration, roster, or API behavior changed."
affected_areas:
  - student-assessments-open-evaluation-feedback
kms_files_updated:
no_impact_reason: "Presentation/error-visibility correction only. It does not change the governed Cycle open contract or any product rule; it makes already-returned backend success/failure state visible at the point of action."
major_change_override: no
